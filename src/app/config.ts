import { environment } from './../environments/environment';

// 切换bim/audit
export const apiPath = environment.apiPath;

// giant --项目API 
export const HOSTURL = environment.HOSTURL;
export const PREFIX = environment.PREFIX;

// spiderbim --文件API
export const IDENTITYURL = environment.IDENTITYURL;
export const FILEURL = environment.FILEURL;
export const CDNFILEURL = environment.CDNFILEURL;
export const version = environment.version;